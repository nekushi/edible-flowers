// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Get a free hosted Postgres database in seconds: `npx create-db`

generator client {
  provider = "prisma-client"
  output   = "../app/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model ImageWithCaption {
  id            String   @id @default(uuid())
  product_title String   @default("no title yet.")
  caption       String   @default("no caption yet.")
  img_url       String
  price         Decimal?
  createdAt     DateTime @default(now())
  updatedAt     DateTime @default(now())
}

// model ClientEmailRequestNotification {
//   id String @id @default(uuid())

// }

model Orders {
  id       String @id @default(uuid())
  order_id String
  title    String
  caption  String
  quantity Int
  img_url  String

  client_email_request ClientEmailRequest @relation(fields: [order_id], references: [orders_id])
  // orders Orders @relation(fields: [orders_id], references: [order_id])

  // client_email_request ClientEmailRequest[]
}

model ClientEmailRequest {
  id        String   @id @default(uuid())
  name      String
  email     String
  message   String
  createdAt DateTime @default(now())

  is_read   Boolean  @default(false)
  orders_id String   @unique
  orders    Orders[]
  // orders Orders @relation(fields: [orders_id], references: [order_id])
}
